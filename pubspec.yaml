name: donuz_dart_sdk
description: Donuz Dart SDK to comunicate with donuz API
version: 0.0.1
homepage:
environment:
  sdk: '>=2.19.2 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: 
  http: 
  intl:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  mocktail:
flutter:
